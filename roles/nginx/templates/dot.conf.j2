server {
    listen 80;
    listen [::]:80;
    server_name dot.{{ DOMAIN }};

    return 301 https://{{ DOMAIN }};
}

server {
    listen       443 ssl http2;
    listen       [::]:443 ssl http2;
    server_name  dot.{{ DOMAIN }};

    ssl_certificate /root/.acme.sh/{{ DOMAIN }}/fullchain.cer;
    ssl_certificate_key /root/.acme.sh/{{ DOMAIN }}/{{ DOMAIN }}.key;

    return 301 https://{{ DOMAIN }};
}